# To run the project 

1) Create a virtual environement. Activate it and navigate to the project directory. 
2) Install requirements with ```pip3 install -r requirements.txt``` in the project directory.
3) cd Backend 
4) then runserver with ```python3 manage.py runserver```
